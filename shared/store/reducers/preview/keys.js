import { PREVIEW_ID } from 'react-native-dotenv'
export const previewKey = `preview:${PREVIEW_ID}`
export const previewKeyUserSelections = `${previewKey}:userSelections`
export const previewKeyGames = `${previewKey}:games`
